一维bose-hubbard模型DMRG(vMPS)算法高精度绘制相图

任务描述：

0、平均场自洽迭代只能给出定性mott叶瓣相图，未能给出一维情况下著名的mott尖峰形状和相边界。遵循原始论文
The one-dimensional Bose-Hubbard Model with nearest-neighbor interaction给出的方法，高精度计算均匀填充的BH相图。

1、双格点DMRG(vMPS)算法基本框架，包括环境收缩 *、双格点粗粒化、构造双格点有效哈密顿矩阵 *、lanczos求解最小本征值/矢 *、SVD截断 *、细粒化、
环境更新、双向扫描* 。  ( 基于硕士课程的海森堡自旋链教学代码， 标有* 的经过个人改编）

2、分别对不同尺寸L,激发excite（=0正常，=+1粒子激发,=-1空穴激发）初始化MPS列表

3、实现粒子数惩罚项MPO、构造Bose-hubbard本身的MPO、实现MPO加法、最终构造带有粒子数惩罚的哈密顿量MPO，实现粒子数守恒以得到正确基态。

4、扫描参数空间，对于每组(t,L,excite)，使用DMRG(vMPS)算法，实现并行计算，求基态能量E，求得在该组(t,L)下的有限尺寸化学势
mu_p=E（excite=1)-E(excite=0), mu_h= E(excite=0)-E(excite=-1)

5、对于每个t，将mu_p和mu_h按照1/L线性拟合（有限尺寸外推），读取截距做为热力学极限下的mu

6、以热力学极限下的mu_p(t)和mu_h(t)做为相边界，绘制高精度相图

7、平衡运算速度和精度必须进行的调参工作

================================================================================